apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "test-api-2"
  description: "API test"
  tags:
    - dotnet
    - csharp
    - webapi
  
  links:
    - url: https://github.com/yvidalv/test-api-2
      title: Repository
      icon: github

  annotations:
    # this could also be `url:<url>` if the documentation isn't in the same location
    github.com/project-slug: yvidalv/test-api-2
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: "group:default/panteon"
  system: system:default/examples
